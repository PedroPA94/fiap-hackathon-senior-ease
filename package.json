{
  "name": "fiap-hackathon-senior-ease",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "start:web": "pnpm --filter @senior-ease/web start"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "latest"
  }
}
